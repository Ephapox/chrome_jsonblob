module.exports = {
  ids: {
    MENU_ID: "jsonblobMenuContainer",
  },
  urls: {
    ORIGIN: `chrome-extension://${chrome.runtime.id}`,
    MENU_URL: `chrome-extension://${chrome.runtime.id}/menu.html`
  }
};
